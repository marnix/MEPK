$[ _base.mm $]

$( $c Nat =N $. $)
$c +N 0N $.

${

        $v n m $.

        $( TODO: add $a for the following:

                (Nat n) ==> (=N (+N n (0N)) n)
                (Nat n) AND (Nat m) ==> (=N (+N n m) (+N m n))
        $)

$}
